# Банк Онлайн 🏦

**Банк Онлайн** — это современная банковская система, разработанная на Java с использованием Spring Boot, Thymeleaf, PostgreSQL, MinIO и Apache POI. Платформа позволяет пользователям управлять своими финансовыми активами онлайн, включая банковские карты, вклады, кредиты и накопления. Для администраторов предусмотрена удобная панель управления резервными копиями базы данных.

## Особенности 🌟

### Пользовательские возможности
- **Регистрация и аутентификация**: простая регистрация и безопасный вход в систему.
- **Главная страница**: просмотр списка всех ваших карт, депозитов, кредитов и накоплений.
- **Управление активами**: открытие новых счетов, внесение денег на счета и получение выписок.
- **Филиалы и банкоматы**: доступ к списку ближайших отделений банка и банкоматов.

### Возможности администратора
- **Автоматическое ежедневное резервирование**: ежедневно в полночь создается резервная копия базы данных и сохраняется в MinIO.
- **Панель управления резервными копиями**: админ-пользователи могут создавать резервные копии вручную, скачивать существующие или восстанавливать базу данных из резервной копии.

### Технические особенности
- **Spring Boot**: мощный фреймворк для быстрой разработки серверных приложений.
- **Thymeleaf**: удобный шаблонизатор HTML-документов.
- **PostgreSQL**: надежная реляционная база данных.
- **MinIO**: объектное хранилище для резервирования баз данных.
- **Flyway**: инструмент миграции базы данных для автоматизации обновлений схемы.
- **Apache POI**: библиотека для экспорта отчетов в Excel-файлы.

## Структура папок 📂

```bash
├── src/main/java/com/bankonline/
│   ├── controller/            # Контроллеры API
│   ├── model/                 # Модели сущностей
│   ├── repository/           # Репозитории Hibernate
│   ├── service/               # Сервисы приложения
│   ├── config/                # Конфигурационные классы
│   ├── util/                  # Вспомогательные классы
│   └── BankBackendApplication # Главный класс приложения
├── src/main/resources/
│   ├── migrations/            # Миграции базы данных
│   ├── static/                # Статические ресурсы
│   │   ├── css/               # CSS-файлы
│   │   └── js/                # JavaScript-файлы
│   └── templates/             # Шаблоны страниц Thymeleaf
│   └── application.properties # Конфигурационный файл приложения
├── gradle
│   └── wrapper/              # Gradle Wrapper
│   └── build.gradle           # Gradle конфигурационный файл
│   └── settings.gradle        # Gradle настройки
│   └── gradlew                # Gradle Wrapper скрипт
│   └── gradlew.bat           # Gradle Wrapper скрипт для Windows
│   └── docker-compose.yml    # Docker Compose файл
│   └── Dockerfile             # Dockerfile
│   └── .gitignore            # Файл игнорируемых файлов
```

## Установка и запуск 🚀

Для запуска системы вам потребуется:

1. Установить Java JDK версии 17.
2. Установить Docker и Docker Compose.
3. Склонируйте репозиторий:

```bash
git clone https://github.com/yourusername/bank-online.git
cd bank-online
```

4. Соберите Docker-образ проекта по Dockerfile

5Запустите контейнеры PostgreSQL, MinIO и веб-сайт с помощью Docker Compose:

```bash
docker-compose up -d
```

Теперь приложение доступно по адресу `http://localhost:8080`